{
  "name": "forge-fields-download",
  "version": "1.0.0",
  "description": "## Description",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "dev": "concurrently \"npm run tailwind\" \"npm run browser-sync\"",
    "tailwind": "npx @tailwindcss/cli -i ./input.css -o ./docs/style.css --watch",
    "browser-sync": "browser-sync start --server \"docs\" --files \"docs/*.html, docs/style.css\" --no-notify"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@tailwindcss/cli": "^4.0.0",
    "tailwindcss": "^4.0.0"
  },
  "devDependencies": {
    "browser-sync": "^3.0.3",
    "concurrently": "^9.1.2"
  }
}
